services:
  mongo:
    image: mongo:7
    container_name: punchme-mongo
    restart: unless-stopped
    ports:
      - '127.0.0.1:27017:27017'
    volumes:
      - punchme-mongo-data:/data/db

volumes:
  punchme-mongo-data:
